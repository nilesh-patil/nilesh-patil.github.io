# Local-dev override. Run with:
#   bundle exec jekyll serve --config _config.yml,_config.dev.yml
# Empties site.url so generated absolute URLs (canonical, og:image,
# masthead asset links, hero overlay background-image) point at the
# local dev host instead of the production github.io URL.
url: ""
